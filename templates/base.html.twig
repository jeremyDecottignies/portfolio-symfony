<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Portfolio DECOTTIGNIES Jérémy{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    {# Bootstrap CDN pour aller vite #}
    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
        rel="stylesheet"
    >
    {% block stylesheets %}{% endblock %}
</head>
<body class="bg-light">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">Mon Portfolio</a>
        <div>
            <a class="nav-link d-inline-block text-white" href="{{ path('app_home') }}">Accueil</a>
            <a class="nav-link d-inline-block text-white" href="{{ path('project_index') }}">Projets</a>
            <a class="nav-link d-inline-block text-white" href="{{ path('app_about') }}">À propos</a>
            <a class="nav-link d-inline-block text-white" href="{{ path('app_contact') }}">Contact</a>
        </div>
    </div>
</nav>

<main class="container mb-5">
    {% block body %}{% endblock %}
</main>

<footer class="text-center text-muted mb-3">
    &copy; {{ "now"|date("Y") }} Ton Nom - BTS SIO
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
{% block javascripts %}{% endblock %}
</body>
</html>
